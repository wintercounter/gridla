/**
 * gridla — pixel-based grid and nested-layout engine.
 *
 * Framework-neutral. Every function takes plain objects and returns new
 * plain objects; nothing here touches the DOM or global state.
 */
export * from './core'
