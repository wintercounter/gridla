/**
 * gridla/dom — vanilla DOM adapter. `mountGrid` turns an element into a
 * canvas: it creates and positions one element per item, wires pointer and
 * keyboard input, measures the canvas, and reports committed layouts. A thin
 * binding over `gridla/interaction`.
 */
export * from './dom/index'
