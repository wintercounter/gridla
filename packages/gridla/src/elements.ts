/**
 * gridla/elements — Web Components adapter. `<gridla-canvas>`, `<gridla-item>`,
 * `<gridla-preview>`, and `<gridla-transfer-scope>` custom elements over
 * `gridla/dom`, registered with `defineGridlaElements`. No shadow DOM; your
 * styles apply.
 */
export * from './elements/index'
