/**
 * gridla/interaction — framework-neutral interaction layer. A controller that
 * owns layout and gesture state for one canvas, a pointer/keyboard state
 * machine over a minimal event shape, a transfer scope for moves between
 * canvases, and element measurement. Adapters (such as `gridla/react`) are
 * thin bindings over this module.
 */
export * from './interaction/index'
