/**
 * gridla/qwik — Qwik adapter. Resumable components over the framework-neutral
 * interaction layer: the server renders the layout from props, the client
 * creates the controller in a visible task. Ships as `dist/qwik.qwik.js` so
 * the consumer's Qwik optimizer processes the `$` boundaries.
 */
export * from './qwik/index'
