/**
 * gridla/solid — Solid adapter. Components written without a compiler
 * (`solid-js/h` on the client, `ssrElement` on the server), fine-grained
 * primitives over the controller store, and transfer scopes, all as thin
 * bindings over `gridla/interaction`.
 */
export * from './solid/index'
