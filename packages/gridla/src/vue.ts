/**
 * gridla/vue — Vue 3 adapter. A provider component that owns layout and
 * gesture state, headless canvas, item, and preview components, a transfer
 * scope, and composables over the store, all as thin bindings over
 * `gridla/interaction`. Written with `defineComponent` and `h`; no compiler
 * step is needed.
 */
export * from './vue/index'
