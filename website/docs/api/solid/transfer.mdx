---
title: Transfer scope
description: Transfer scope exports of gridla/solid. Generated from the source declarations.
---

# Transfer scope

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Move items between providers. Wrap several [`GridProvider`](/api/qwik/provider#gridprovider)s in a [`GridTransferScope`](/api/qwik/transfer#gridtransferscope); the pointer decides the target and the provider callbacks report the transfer.

```ts
import { GridTransferScope } from 'gridla/solid'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GridTransferScope`](#gridtransferscope) | component | Lets items move between every `GridProvider` rendered inside it. |

## Components

### GridTransferScope

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/transfer.ts#L12">solid/transfer.ts:12</a></span>

Lets items move between every [`GridProvider`](/api/qwik/provider#gridprovider) rendered inside it. The
pointer decides the target: the deepest registered canvas under the pointer
that accepts the item previews the drop; releasing there commits it.
Wraps a [`TransferScope`](/api/elements/elements#transferscope) from `gridla/interaction` in context.

```ts
export function GridTransferScope(props: { children?: JSX.Element }): JSX.Element
```
