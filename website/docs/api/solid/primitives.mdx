---
title: Primitives
description: Primitives exports of gridla/solid. Generated from the source declarations.
---

# Primitives

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Accessors over slices of provider state built with `from()` over the controller store, plus the imperative actions. Each accessor notifies only when its slice changes.

```ts
import { useGridActions, useGridInteractionState, useGridItem, useGridItemView, useGridLayout, useGridPreview, useGridSelection, useGridSourceLayout, useGridStore, useGridVisibleLayout } from 'gridla/solid'
import type { GridItemView, MaybeAccessor } from 'gridla/solid'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GridItemView`](#griditemview) | type | Everything needed to paint one item: its current and pre-gesture rectangles plus its active, selected, shifted, and transferring flags. |
| [`MaybeAccessor`](#maybeaccessor) | type | A plain value or an accessor for it. |
| [`useGridActions`](#usegridactions) | hook | Imperative layout and selection actions. |
| [`useGridInteractionState`](#usegridinteractionstate) | hook | The gesture in progress, or `null` when idle. |
| [`useGridItem`](#usegriditem) | hook | One item as it should be painted right now. |
| [`useGridItemView`](#usegriditemview) | hook | Everything a rendered item needs, as one accessor that updates only when the view changes. |
| [`useGridLayout`](#usegridlayout) | hook | The rendered layout (projected onto the measured canvas size). |
| [`useGridPreview`](#usegridpreview) | hook | The solver's latest preview for the gesture in progress, or `null` when idle. |
| [`useGridSelection`](#usegridselection) | hook | Id of the selected item, or `null` when nothing is selected. |
| [`useGridSourceLayout`](#usegridsourcelayout) | hook | The layout the provider was given, in its own coordinates. |
| [`useGridStore`](#usegridstore) | hook | Subscribe to a slice of provider state. |
| [`useGridVisibleLayout`](#usegridvisiblelayout) | hook | The layout that should be painted right now: the preview during a gesture, else the rendered layout. |

## Hooks

### useGridActions

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L57">solid/hooks.ts:57</a></span>

Imperative layout and selection actions. Stable for the provider's lifetime.

```ts
export function useGridActions<TData = unknown>(): GridActions<TData>
```

### useGridInteractionState

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L141">solid/hooks.ts:141</a></span>

The gesture in progress, or `null` when idle.

```ts
export function useGridInteractionState(): Accessor<GridInteraction | null>
```

### useGridItem

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L77">solid/hooks.ts:77</a></span>

One item as it should be painted right now. `null` when absent.

```ts
export function useGridItem<TData = unknown>(
  itemId: MaybeAccessor<string>,
): Accessor<GridItem<TData> | null>
```

### useGridItemView

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L108">solid/hooks.ts:108</a></span>

Everything a rendered item needs, as one accessor that updates only when the view changes.

```ts
export function useGridItemView<TData = unknown>(
  itemId: MaybeAccessor<string>,
): Accessor<GridItemView>
```

### useGridLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L62">solid/hooks.ts:62</a></span>

The rendered layout (projected onto the measured canvas size).

```ts
export function useGridLayout<TData = unknown>(): Accessor<GridLayout<TData>>
```

### useGridPreview

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L146">solid/hooks.ts:146</a></span>

The solver's latest preview for the gesture in progress, or `null` when idle.

```ts
export function useGridPreview<TData = unknown>(): Accessor<GridPreview<TData> | null>
```

### useGridSelection

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L151">solid/hooks.ts:151</a></span>

Id of the selected item, or `null` when nothing is selected.

```ts
export function useGridSelection(): Accessor<string | null>
```

### useGridSourceLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L67">solid/hooks.ts:67</a></span>

The layout the provider was given, in its own coordinates.

```ts
export function useGridSourceLayout<TData = unknown>(): Accessor<GridLayout<TData>>
```

### useGridStore

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L22">solid/hooks.ts:22</a></span>

Subscribe to a slice of provider state. Returns an accessor that notifies
only when the selected value changes (by `Object.is`, or `isEqual`). Built
with `from()` over the controller store, so it follows Solid's ownership
rules and unsubscribes with the component.

```ts
export function useGridStore<TData = unknown, TSlice = GridState<TData>>(
  selector: (state: GridState<TData>) => TSlice = (state) => state as unknown as TSlice,
  isEqual: (a: TSlice, b: TSlice) => boolean = Object.is,
): Accessor<TSlice>
```

### useGridVisibleLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L72">solid/hooks.ts:72</a></span>

The layout that should be painted right now: the preview during a gesture, else the rendered layout.

```ts
export function useGridVisibleLayout<TData = unknown>(): Accessor<GridLayout<TData>>
```

## Types

### GridItemView

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L91">solid/hooks.ts:91</a></span>

Everything needed to paint one item: its current and pre-gesture rectangles
plus its active, selected, shifted, and transferring flags. Returned by
[`useGridItemView`](/api/qwik/hooks#usegriditemview).

```ts
export type GridItemView = {
  /** Where the item is painted right now (preview-aware). */
  rect: GridRect
  /** Where the item was before the current gesture. */
  baseRect: GridRect
  /** Cursor-tracked rect while this item is active; `null` otherwise. */
  activeRect: GridRect | null
  isActive: boolean
  isSelected: boolean
  /** True when this item moved in the preview because another item pushed it. */
  isShifted: boolean
  /** True while the active item is being previewed in another canvas. */
  isTransferring: boolean
  interaction: GridInteraction | null
}
```

| Member | Type | Description |
| --- | --- | --- |
| `rect` | `GridRect` | Where the item is painted right now (preview-aware). |
| `baseRect` | `GridRect` | Where the item was before the current gesture. |
| `activeRect` | `GridRect \\| null` | Cursor-tracked rect while this item is active; `null` otherwise. |
| `isActive` | `boolean` |  |
| `isSelected` | `boolean` |  |
| `isShifted` | `boolean` | True when this item moved in the preview because another item pushed it. |
| `isTransferring` | `boolean` | True while the active item is being previewed in another canvas. |
| `interaction` | `GridInteraction \\| null` |  |

### MaybeAccessor

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/solid/hooks.ts#L10">solid/hooks.ts:10</a></span>

A plain value or an accessor for it. Item ids may be given either way.

```ts
export type MaybeAccessor<T> = T | Accessor<T>
```
