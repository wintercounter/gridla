---
title: Provider
description: Provider exports of gridla/angular. Generated from the source declarations.
---

# Provider

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

[`GridProviderComponent`](#gridprovidercomponent) (`<gridla-provider>` or `[gridlaProvider]`) owns layout and gesture state for one canvas and provides an injectable [`GridController`](#gridcontroller) to its content. [`provideGridla`](#providegridla) registers application-wide defaults.

```ts
import { GRID_TRANSFER_SCOPE, GridController, GRIDLA_OPTIONS, GridProviderComponent, provideGridla, provideGridTransferScope } from 'gridla/angular'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GRID_TRANSFER_SCOPE`](#grid_transfer_scope) | const | Injection token for the `TransferScope` a provider registers with. |
| [`GridController`](#gridcontroller) | class | Injectable owner of one canvas: a `createGridController` handle from `gridla/interaction` wrapped in signals. |
| [`GRIDLA_OPTIONS`](#gridla_options) | const | Injection token for the application-wide defaults registered with `provideGridla`. |
| [`GridProviderComponent`](#gridprovidercomponent) | component | Owns layout state and gesture state for one canvas. |
| [`provideGridla`](#providegridla) | function | Register application-wide defaults for every `gridlaProvider`: solver options (`gap`, `snapDistance`, `snap`, `onTrace`) plus `responsive`, `dragThreshold`, and `keyboardStep`. |
| [`provideGridTransferScope`](#providegridtransferscope) | function | Providers for a transfer scope. |

## Functions

### provideGridla

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/provide.ts#L32">angular/provide.ts:32</a></span>

Register application-wide defaults for every `gridlaProvider`: solver
options (`gap`, `snapDistance`, `snap`, `onTrace`) plus `responsive`,
`dragThreshold`, and `keyboardStep`. Add it to `bootstrapApplication` or a
route's `providers`. Optional: providers work without it.

```ts
export function provideGridla(options: GridlaOptions = {}): EnvironmentProviders
```

### provideGridTransferScope

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/provide.ts#L41">angular/provide.ts:41</a></span>

Providers for a transfer scope. [`GridTransferScopeComponent`](/api/angular/components#gridtransferscopecomponent) uses this; add
it to a component's `providers` to make every `gridlaProvider` inside that
component share one scope without an extra element.

```ts
export function provideGridTransferScope(): Provider[]
```

## Components

### GridProviderComponent

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/provider.component.ts#L34">angular/provider.component.ts:34</a></span>

Owns layout state and gesture state for one canvas. Use it as an element
(`\<gridla-provider\>`) or as an attribute on any element (`[gridlaProvider]`);
it provides a [`GridController`](#gridcontroller) to everything inside. Place a
`\<gridla-canvas\>` inside it. Nested layouts are nested providers.

Controlled: bind `[layout]` and listen to `(layoutChange)`, or use
`[(layout)]`. Uncontrolled: pass `[defaultLayout]`. Every [`SolveOptions`](/api/core/solvers#solveoptions)
field is an input; `[config]` takes them as one object.

```ts
export class GridProviderComponent<TData = unknown> implements OnInit // selector: gridla-provider, [gridlaProvider]
```

| Member | Type | Description |
| --- | --- | --- |
| `controller` | `GridController\<TData\>` | The controller this provider owns. |
| `layout` | `InputSignal\<GridLayout\<TData\> \\| undefined\>` | Controlled layout. Pair with `(layoutChange)` or bind `[(layout)]`. |
| `defaultLayout` | `InputSignal\<GridLayout\<TData\> \\| undefined\>` | Initial layout for uncontrolled use. |
| `config` | `InputSignal\<GridlaOptions \\| undefined\>` | Solver options and controller settings as one object. Individual inputs win. |
| `responsive` | `InputSignalWithTransform\<boolean \\| undefined, unknown\>` | Project the layout onto the measured canvas size. Default `true`. |
| `dragThreshold` | `InputSignalWithTransform\<number \\| undefined, unknown\>` | Minimum pointer travel before a press becomes a drag. Default `4`. |
| `keyboardStep` | `InputSignalWithTransform\<number \\| undefined, unknown\>` | Pixels moved per arrow key press. Default `8`; Shift multiplies by 4. |
| `gap` | `InputSignalWithTransform\<number \\| undefined, unknown\>` | Minimum distance kept between items. Default `0`. |
| `snapDistance` | `InputSignalWithTransform\<number \\| undefined, unknown\>` | Distance within which edges snap to neighbors. |
| `snap` | `InputSignalWithTransform\<boolean \\| undefined, unknown\>` | Alignment snapping on or off. |
| `onTrace` | `InputSignal\<TraceCallback \\| undefined\>` | Solver tracing callback. |
| `selectedId` | `InputSignal\<string \\| null \\| undefined\>` | Controlled selection. Pair with `(selectedIdChange)` or bind `[(selectedId)]`. |
| `acceptTransfers` | `InputSignalWithTransform\<boolean \\| ((item: GridItem\<TData\>, sourceId: string) =\> boolean) \\| undefined, unknown\>` | Whether items from other canvases may be dropped here. Default `true`. |
| `layoutChange` | `OutputEmitterRef\<GridLayout\<TData\>\>` | The next layout after every accepted change. Enables `[(layout)]`. |
| `layoutChangeDetail` | `OutputEmitterRef\<GridLayoutChangeEvent\<TData\>\>` | The next layout together with the `GridChangeDetail` that produced it. |
| `commit` | `OutputEmitterRef\<GridChangeDetail\>` | The solver strategy of every accepted interactive commit. |
| `selectedIdChange` | `OutputEmitterRef\<string \\| null\>` | Selection changes. Enables `[(selectedId)]`. |
| `transferIn` | `OutputEmitterRef\<GridTransferInEvent\<TData\>\>` | An item arrived from another canvas inside a transfer scope. |
| `transferOut` | `OutputEmitterRef\<GridTransferOutEvent\>` | An item left for another canvas inside a transfer scope. |
| `ngOnInit` | `() =\> void` | Seeds an uncontrolled layout and applies the initial options. |

## Classes

### GridController

<span className="g-api-meta">class · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/controller.ts#L37">angular/controller.ts:37</a></span>

Injectable owner of one canvas: a [`createGridController`](/api/interaction/controller#creategridcontroller) handle from
`gridla/interaction` wrapped in signals. [`GridProviderComponent`](#gridprovidercomponent) provides
one instance per provider and forwards its inputs; children inject it with
`inject(GridController)` (or the `injectGrid*` helpers) to read state and
call actions. Standalone use: add it to a component's `providers` and call
`setOptions` yourself.

```ts
export class GridController<TData = unknown> implements OnDestroy
```

| Member | Type | Description |
| --- | --- | --- |
| `handle` | `GridControllerHandle\<TData\>` | The underlying framework-neutral controller. |
| `id` | `string` | Id of this controller. Used by transfer scopes. |
| `store` | `GridStore\<GridState\<TData\>\>` | State store. The signals below are derived from it. |
| `actions` | `GridActions\<TData\>` | Imperative layout and selection API. Stable for the controller's lifetime. |
| `gesture` | `GridGestureApi\<TData\>` | Low-level gesture API. Stable for the controller's lifetime. |
| `state` | `Signal\<GridState\<TData\>\>` | The whole controller state as a signal. |
| `layout` | `Signal\<GridLayout\<TData\>\>` | The rendered layout (projected onto the measured canvas size). |
| `sourceLayout` | `Signal\<GridLayout\<TData\>\>` | The layout the provider was given, in its own coordinates. |
| `visibleLayout` | `Signal\<GridLayout\<TData\>\>` | The layout to paint right now: the preview during a gesture, else the rendered layout. |
| `interaction` | `Signal\<GridInteraction \\| null\>` | The gesture in progress, or `null` when idle. |
| `preview` | `Signal\<GridPreview\<TData\> \\| null\>` | The solver's latest preview for the gesture in progress, or `null` when idle. |
| `selectedId` | `Signal\<string \\| null\>` | Id of the selected item, or `null`. |
| `dragging` | `Signal\<boolean\>` | True while a move or resize is in progress. |
| `select` | `\<TSlice\>(selector: (state: GridState\<TData\>) =\> TSlice, equal?: ValueEqualityFn\<TSlice\>) =\> Signal\<TSlice\>` | A signal over a slice of the state. Recomputes on every store change and notifies dependents only when `equal` (default `Object.is`) says the slice changed. |
| `itemView` | `(itemId: string \\| Signal\<string\>) =\> Signal\<GridItemView\>` | Everything needed to paint one item, with structural equality. |
| `getConfig` | `() =\> GridControllerConfig` | The resolved configuration currently in effect. |
| `setOptions` | `(options: GridControllerOptions\<TData\>) =\> void` | Apply options: callbacks, config fields, the controlled `layout` and `selectedId`. Application defaults from `provideGridla` fill in fields left `undefined`. `GridProviderComponent` calls this from an effect over its inputs. |
| `setLayout` | `(layout: GridLayout\<TData\>) =\> void` | Sync a layout into the store without reporting a change (used for `defaultLayout`). |
| `setSize` | `(size: GridSize \\| null) =\> void` | Update the measured canvas size. `GridCanvasComponent` calls this. |
| `ngOnDestroy` | `() =\> void` | Stops the store subscription and destroys the underlying controller. |

## Constants

### GRID_TRANSFER_SCOPE

<span className="g-api-meta">const · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/provide.ts#L24">angular/provide.ts:24</a></span>

Injection token for the [`TransferScope`](/api/elements/elements#transferscope) a provider registers with.
[`GridTransferScopeComponent`](/api/angular/components#gridtransferscopecomponent) provides it; inject it optionally to
participate in transfers from custom code.

```ts
export const GRID_TRANSFER_SCOPE: InjectionToken<TransferScope>
```

### GRIDLA_OPTIONS

<span className="g-api-meta">const · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/provide.ts#L17">angular/provide.ts:17</a></span>

Injection token for the application-wide defaults registered with
[`provideGridla`](#providegridla). Inject it to read the defaults; provide it directly when
[`provideGridla`](#providegridla) is not an option (for example inside a component's
`providers` array).

```ts
export const GRIDLA_OPTIONS: InjectionToken<GridlaOptions>
```
