---
title: Signals
description: Signals exports of gridla/angular. Generated from the source declarations.
---

# Signals

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Read provider state as signals from any injection context: a slice of the state, one item as it should be painted, or the imperative actions.

```ts
import { injectGridActions, injectGridController, injectGridItemView, injectGridStore, itemViewsEqual, selectItemView } from 'gridla/angular'
import type { GridItemView } from 'gridla/angular'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GridItemView`](#griditemview) | type | Everything needed to paint one item: its current and pre-gesture rectangles plus its active, selected, shifted, and transferring flags. |
| [`injectGridActions`](#injectgridactions) | function | Imperative layout and selection actions of the nearest provider. |
| [`injectGridController`](#injectgridcontroller) | function | The `GridController` of the nearest enclosing provider. |
| [`injectGridItemView`](#injectgriditemview) | function | Everything a rendered item needs, as a signal with structural equality. |
| [`injectGridStore`](#injectgridstore) | function | A signal over a slice of the nearest provider's state. |
| [`itemViewsEqual`](#itemviewsequal) | function | Structural equality for `GridItemView`, used to skip redundant signal updates. |
| [`selectItemView`](#selectitemview) | function | Derive the `GridItemView` of `itemId` from controller state. |

## Functions

### injectGridActions

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/inject.ts#L37">angular/inject.ts:37</a></span>

Imperative layout and selection actions of the nearest provider. Call it in an injection context.

```ts
export function injectGridActions<TData = unknown>(): GridActions<TData>
```

### injectGridController

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/inject.ts#L11">angular/inject.ts:11</a></span>

The [`GridController`](/api/angular/provider#gridcontroller) of the nearest enclosing provider. Call it in an
injection context (a constructor or field initializer).

```ts
export function injectGridController<TData = unknown>(): GridController<TData>
```

### injectGridItemView

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/inject.ts#L32">angular/inject.ts:32</a></span>

Everything a rendered item needs, as a signal with structural equality.
Pass a static id or a signal (for example an `input`). Call it in an
injection context.

```ts
export function injectGridItemView(itemId: string | Signal<string>): Signal<GridItemView>
```

### injectGridStore

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/inject.ts#L20">angular/inject.ts:20</a></span>

A signal over a slice of the nearest provider's state. Recomputes on every
store change and notifies dependents only when `equal` (default `Object.is`)
says the slice changed. Call it in an injection context.

```ts
export function injectGridStore<TSlice, TData = unknown>(
  selector: (state: GridState<TData>) => TSlice,
  equal?: ValueEqualityFn<TSlice>,
): Signal<TSlice>
```

### itemViewsEqual

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/view.ts#L34">angular/view.ts:34</a></span>

Structural equality for [`GridItemView`](/api/qwik/hooks#griditemview), used to skip redundant signal updates.

```ts
export function itemViewsEqual(a: GridItemView, b: GridItemView): boolean
```

### selectItemView

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/view.ts#L48">angular/view.ts:48</a></span>

Derive the [`GridItemView`](/api/qwik/hooks#griditemview) of `itemId` from controller state.

```ts
export function selectItemView<TData>(state: GridState<TData>, itemId: string): GridItemView
```

## Types

### GridItemView

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/angular/view.ts#L11">angular/view.ts:11</a></span>

Everything needed to paint one item: its current and pre-gesture rectangles
plus its active, selected, shifted, and transferring flags. Returned by
[`injectGridItemView`](#injectgriditemview) and `GridController.itemView`.

```ts
export type GridItemView = {
  /** Where the item is painted right now (preview-aware). */
  rect: GridRect
  /** Where the item was before the current gesture. */
  baseRect: GridRect
  /** Cursor-tracked rect while this item is active; `null` otherwise. */
  activeRect: GridRect | null
  isActive: boolean
  isSelected: boolean
  /** True when this item moved in the preview because another item pushed it. */
  isShifted: boolean
  /** True while the active item is being previewed in another canvas. */
  isTransferring: boolean
  interaction: GridInteraction | null
}
```

| Member | Type | Description |
| --- | --- | --- |
| `rect` | `GridRect` | Where the item is painted right now (preview-aware). |
| `baseRect` | `GridRect` | Where the item was before the current gesture. |
| `activeRect` | `GridRect \\| null` | Cursor-tracked rect while this item is active; `null` otherwise. |
| `isActive` | `boolean` |  |
| `isSelected` | `boolean` |  |
| `isShifted` | `boolean` | True when this item moved in the preview because another item pushed it. |
| `isTransferring` | `boolean` | True while the active item is being previewed in another canvas. |
| `interaction` | `GridInteraction \\| null` |  |
