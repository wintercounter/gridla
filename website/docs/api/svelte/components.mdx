---
title: Components
description: Components exports of gridla/svelte. Generated from the source declarations.
---

# Components

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Svelte 5 components over the interaction layer. [`GridProvider`](/api/qwik/provider#gridprovider) owns layout and gesture state for one canvas (`bind:layout` or `defaultLayout`); [`GridCanvas`](/api/qwik/components#gridcanvas) measures itself and wires input; [`GridItem`](/api/qwik/components#griditem) positions one item and passes its view to the children snippet; [`GridPreviewOutline`](/api/qwik/components#gridpreviewoutline) shows where the active item will land; [`GridTransferScope`](/api/qwik/transfer#gridtransferscope) lets items move between providers.

```ts
import { GridCanvas, GridItem, GridPreviewOutline, GridProvider, GridTransferScope } from 'gridla/svelte'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GridCanvas`](#gridcanvas) | component | The element items are positioned in. |
| [`GridItem`](#griditem) | component | Positions one item inside `GridCanvas`. |
| [`GridPreviewOutline`](#gridpreviewoutline) | component | Renders a box where the active item will land when released. |
| [`GridProvider`](#gridprovider) | component | Owns layout and gesture state for one canvas. |
| [`GridTransferScope`](#gridtransferscope) | component | Lets items move between every `GridProvider` rendered inside it. |

## Components

### GridCanvas

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/GridCanvas.svelte">svelte/GridCanvas.svelte</a></span>

The element items are positioned in. Measures itself, feeds the size to the
provider, and wires pointer and keyboard handling. Renders a `div` with
`position: relative`; give it a height (or let it follow the layout with
`responsive=\{false\}` on the provider).

```ts
export const GridCanvas: Component<GridCanvasProps, {}, "">
```

### GridItem

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/GridItem.svelte">svelte/GridItem.svelte</a></span>

Positions one item inside [`GridCanvas`](/api/qwik/components#gridcanvas). Headless: it renders a `div` with
geometry styles and data attributes and leaves appearance to you. The
children snippet receives the item view plus drag and resize handle
attributes.

```ts
export const GridItem: Component<GridItemProps, {}, "">
```

### GridPreviewOutline

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/GridPreviewOutline.svelte">svelte/GridPreviewOutline.svelte</a></span>

Renders a box where the active item will land when released. Renders nothing
when no gesture is in progress.

```ts
export const GridPreviewOutline: Component<GridPreviewOutlineProps, {}, "">
```

### GridProvider

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/GridProvider.svelte">svelte/GridProvider.svelte</a></span>

Owns layout and gesture state for one canvas. Place a [`GridCanvas`](/api/qwik/components#gridcanvas) inside it.
Works controlled (`layout` or `bind:layout`, with `onLayoutChange`) or
uncontrolled (`defaultLayout`). A binding over [`createGridController`](/api/interaction/controller#creategridcontroller) from
`gridla/interaction`; nested layouts are nested providers.

```ts
export const GridProvider: $$IsomorphicComponent
```

### GridTransferScope

<span className="g-api-meta">component · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/GridTransferScope.svelte">svelte/GridTransferScope.svelte</a></span>

Lets items move between every [`GridProvider`](/api/qwik/provider#gridprovider) rendered inside it. The pointer
decides the target: the deepest registered canvas under the pointer that
accepts the item previews the drop; releasing there commits it. Wraps a
[`TransferScope`](/api/elements/elements#transferscope) from `gridla/interaction` in context.

```ts
export const GridTransferScope: Component<GridTransferScopeProps, {}, "">
```
