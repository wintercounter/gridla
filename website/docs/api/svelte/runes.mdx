---
title: Runes
description: Runes exports of gridla/svelte. Generated from the source declarations.
---

# Runes

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Rune-style readers over the nearest provider, called during component initialization: [`gridStore`](#gridstore) selects a slice of state, [`gridItemView`](#griditemview) follows one item, [`gridLayout`](#gridlayout) and [`gridSelection`](#gridselection) read the rendered layout and the selection, and [`gridActions`](#gridactions) reaches the imperative API. [`createGridRunes`](#creategridrunes) wraps a controller store in `$state.raw` for custom providers.

```ts
import { createGridRunes, getGridContext, getTransferScopeContext, gridActions, gridItemView, gridLayout, gridSelection, gridStore, itemViewsEqual, rectsEqual, rectStyle, resizeHandleStyle, selectItemView, setGridContext, setTransferScopeContext } from 'gridla/svelte'
import type { GridRead, GridRunes } from 'gridla/svelte'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`createGridRunes`](#creategridrunes) | function | Create a `GridController` and wrap its store in `$state.raw`. |
| [`getGridContext`](#getgridcontext) | function | Read the nearest `GridProvider`'s runes. |
| [`getTransferScopeContext`](#gettransferscopecontext) | function | The nearest `GridTransferScope`'s scope, or `null` when there is none. |
| [`gridActions`](#gridactions) | function | Imperative layout and selection actions of the nearest provider. |
| [`gridItemView`](#griditemview) | function | Everything a rendered item needs, updated only when its view changes. |
| [`gridLayout`](#gridlayout) | function | The rendered layout (projected onto the measured canvas size) of the nearest provider. |
| [`GridRead`](#gridread) | type | A reactive read-only value. |
| [`GridRunes`](#gridrunes) | type | A `GridController` whose state is exposed as a rune. |
| [`gridSelection`](#gridselection) | function | Id of the selected item in the nearest provider, or `null` when nothing is selected. |
| [`gridStore`](#gridstore) | function | Select a slice of the nearest provider's state. |
| [`itemViewsEqual`](#itemviewsequal) | function | Structural equality of two item views; used to skip redundant updates. |
| [`rectsEqual`](#rectsequal) | function | Structural equality of two rects (or both `null`). |
| [`rectStyle`](#rectstyle) | function | Inline style that places an element at `rect` inside a canvas. |
| [`resizeHandleStyle`](#resizehandlestyle) | function | Inline style of a built-in resize handle. |
| [`selectItemView`](#selectitemview) | function | Derive the `GridItemView` of `itemId` from a controller state snapshot. |
| [`setGridContext`](#setgridcontext) | function | Provide `runes` to descendant components. |
| [`setTransferScopeContext`](#settransferscopecontext) | function | Provide a `TransferScope` to descendant providers. |

## Functions

### createGridRunes

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Create a [`GridController`](/api/angular/provider#gridcontroller) and wrap its store in `$state.raw`. Use it to
build a custom provider; [`GridProvider`](/api/qwik/provider#gridprovider) calls it for you. Call `destroy`
when the owning component unmounts.

```ts
export declare function createGridRunes<TData = unknown>(options?: GridControllerOptions<TData>): GridRunes<TData>;
```

### getGridContext

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Read the nearest [`GridProvider`](/api/qwik/provider#gridprovider)'s runes. Call during component
initialization; throws when no provider is above the component.

```ts
export declare function getGridContext<TData = unknown>(): GridRunes<TData>;
```

### getTransferScopeContext

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

The nearest [`GridTransferScope`](/api/qwik/transfer#gridtransferscope)'s scope, or `null` when there is none.

```ts
export declare function getTransferScopeContext(): TransferScope | null;
```

### gridActions

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Imperative layout and selection actions of the nearest provider. Stable for its lifetime.

```ts
export declare function gridActions<TData = unknown>(): GridActions<TData>;
```

### gridItemView

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Everything a rendered item needs, updated only when its view changes. Pass
a getter to follow a reactive id.

```ts
export declare function gridItemView<TData = unknown>(itemId: string | (() => string)): GridRead<GridItemView>;
```

### gridLayout

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

The rendered layout (projected onto the measured canvas size) of the nearest provider.

```ts
export declare function gridLayout<TData = unknown>(): GridRead<GridLayout<TData>>;
```

### gridSelection

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Id of the selected item in the nearest provider, or `null` when nothing is selected.

```ts
export declare function gridSelection(): GridRead<string | null>;
```

### gridStore

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Select a slice of the nearest provider's state. `current` recomputes when
the state changes and keeps its previous value while `isEqual` (default
`Object.is`) reports the new slice equal, so dependents stay quiet.

```ts
export declare function gridStore<TData = unknown, TSlice = GridState<TData>>(selector?: (state: GridState<TData>) => TSlice, isEqual?: (a: TSlice, b: TSlice) => boolean): GridRead<TSlice>;
```

### itemViewsEqual

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/view.ts">svelte/view.ts</a></span>

Structural equality of two item views; used to skip redundant updates.

```ts
export declare function itemViewsEqual(a: GridItemView, b: GridItemView): boolean;
```

### rectsEqual

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/view.ts">svelte/view.ts</a></span>

Structural equality of two rects (or both `null`).

```ts
export declare function rectsEqual(a: GridRect | null, b: GridRect | null): boolean;
```

### rectStyle

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/view.ts">svelte/view.ts</a></span>

Inline style that places an element at `rect` inside a canvas.

```ts
export declare function rectStyle(rect: GridRect, positioning: 'transform' | 'absolute'): string;
```

### resizeHandleStyle

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/view.ts">svelte/view.ts</a></span>

Inline style of a built-in resize handle. Handles sit fully inside the item
so they stay hit-testable when the item clips its overflow; the geometry
reads `--gridla-handle-size` and `--gridla-handle-inset` so CSS can resize
them. `size` is the fallback thickness.

```ts
export declare function resizeHandleStyle(edge: GridResizeEdge, size?: number): string;
```

### selectItemView

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/view.ts">svelte/view.ts</a></span>

Derive the [`GridItemView`](/api/qwik/hooks#griditemview) of `itemId` from a controller state snapshot.

```ts
export declare function selectItemView<TData>(state: GridState<TData>, itemId: string): GridItemView;
```

### setGridContext

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Provide `runes` to descendant components. Call during component initialization.

```ts
export declare function setGridContext<TData>(runes: GridRunes<TData>): GridRunes<TData>;
```

### setTransferScopeContext

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

Provide a [`TransferScope`](/api/elements/elements#transferscope) to descendant providers. [`GridTransferScope`](/api/qwik/transfer#gridtransferscope) calls this.

```ts
export declare function setTransferScopeContext(scope: TransferScope): TransferScope;
```

## Types

### GridRead

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

A reactive read-only value. Read `current` inside a template, `$derived`, or `$effect`.

```ts
export type GridRead<T> = {
    readonly current: T;
};
```

| Member | Type | Description |
| --- | --- | --- |
| `current` | `T` |  |

### GridRunes

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/svelte/context.svelte.ts">svelte/context.svelte.ts</a></span>

A [`GridController`](/api/angular/provider#gridcontroller) whose state is exposed as a rune. `state` is a
`$state.raw` snapshot that follows the controller store, so reading it in a
component, `$derived`, or `$effect` tracks every change. Created by
[`createGridRunes`](#creategridrunes) and provided to descendants by [`GridProvider`](/api/qwik/provider#gridprovider).

```ts
export type GridRunes<TData = unknown> = {
    /** Id of the controller. Used by transfer scopes. */
    id: string;
    /** The controller the runes wrap. */
    controller: GridController<TData>;
    /** Current controller state (reactive). */
    readonly state: GridState<TData>;
    /** Resolved configuration in effect (reactive). */
    readonly config: GridControllerConfig;
    /** Imperative layout and selection API. Stable for the controller's lifetime. */
    actions: GridActions<TData>;
    /** Low-level gesture API. Stable for the controller's lifetime. */
    gesture: GridGestureApi<TData>;
    /** Forward changed options to the controller and refresh `config`. */
    setOptions: (options: GridControllerOptions<TData>) => void;
    /** Stop following the store and destroy the controller. */
    destroy: () => void;
};
```

| Member | Type | Description |
| --- | --- | --- |
| `id` | `string` | Id of the controller. Used by transfer scopes. |
| `controller` | `GridController\<TData\>` | The controller the runes wrap. |
| `state` | `GridState\<TData\>` | Current controller state (reactive). |
| `config` | `GridControllerConfig` | Resolved configuration in effect (reactive). |
| `actions` | `GridActions\<TData\>` | Imperative layout and selection API. Stable for the controller's lifetime. |
| `gesture` | `GridGestureApi\<TData\>` | Low-level gesture API. Stable for the controller's lifetime. |
| `setOptions` | `(options: GridControllerOptions\<TData\>) =\> void` | Forward changed options to the controller and refresh `config`. |
| `destroy` | `() =\> void` | Stop following the store and destroy the controller. |
