---
title: Nested
description: Nested exports of gridla. Generated from the source declarations.
---

# Nested

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Trees of layouts. [`flattenLayout`](#flattenlayout) projects every container into the rectangle its parent assigned it and returns root-relative rectangles; the rest are queries, coordinate conversions, and compaction over that flat result.

```ts
import {
  compactLayout,
  findContainerAt,
  findFirstUnlockedAncestor,
  flattenLayout,
  getAncestors,
  getDescendants,
  gridNodeAdapter,
  hitTest,
  isDirectChildOfContained,
  isInsideLockedSubtree,
  markLockedItems,
  projectItemsToRoot,
  projectItemToRoot,
  renderLayoutForRect,
  rootPointToContainer,
  scaleSizeBetweenContainers,
  toRenderedLayout,
} from 'gridla'
import type {
  CompactOptions,
  CompactResult,
  FindContainerOptions,
  FlatItem,
  FlatLayout,
  FlattenOptions,
  GridNode,
  GridNodeBehavior,
  GridTreeAdapter,
} from 'gridla'
```

| Export                                                      | Kind     | Summary                                                                                                                                                                          |
| ----------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`compactLayout`](#compactlayout)                           | function | Shrink items vertically until the layout fits a bounded canvas.                                                                                                                  |
| [`CompactOptions`](#compactoptions)                         | type     | Options for `compactLayout`.                                                                                                                                                     |
| [`CompactResult`](#compactresult)                           | type     | Result of `compactLayout`: the compacted layout and whether everything fit.                                                                                                      |
| [`findContainerAt`](#findcontainerat)                       | function | Deepest container that accepts children and contains the point.                                                                                                                  |
| [`FindContainerOptions`](#findcontaineroptions)             | type     | Options for `findContainerAt`.                                                                                                                                                   |
| [`findFirstUnlockedAncestor`](#findfirstunlockedancestor)   | function | Nearest ancestor (or the item itself) that is outside every locked subtree.                                                                                                      |
| [`FlatItem`](#flatitem)                                     | type     | One node of a flattened tree: its rectangle in root coordinates, its place in the hierarchy, its rendered and authored layouts, and its resolved behavior flags.                 |
| [`FlatLayout`](#flatlayout)                                 | type     | Result of `flattenLayout`: every node in paint order (each parent before its children) plus lookups by id and by parent id.                                                      |
| [`flattenLayout`](#flattenlayout)                           | function | Flatten a tree of nested layouts into root-relative rectangles.                                                                                                                  |
| [`FlattenOptions`](#flattenoptions)                         | type     | Options for `flattenLayout`.                                                                                                                                                     |
| [`getAncestors`](#getancestors)                             | function | Ancestors of an item from parent to root.                                                                                                                                        |
| [`getDescendants`](#getdescendants)                         | function | All descendants of an item in paint order.                                                                                                                                       |
| [`GridNode`](#gridnode)                                     | type     | Normalized tree node.                                                                                                                                                            |
| [`gridNodeAdapter`](#gridnodeadapter)                       | const    | The `GridTreeAdapter` for plain `GridNode` trees.                                                                                                                                |
| [`GridNodeBehavior`](#gridnodebehavior)                     | type     | Per-node flags that control how a node takes part in nesting: whether it is a container, accepts drops, confines its children, is locked, or keeps its height during compaction. |
| [`GridTreeAdapter`](#gridtreeadapter)                       | type     | Callbacks that let `flattenLayout` read any tree shape without conversion.                                                                                                       |
| [`hitTest`](#hittest)                                       | function | Deepest item whose rect contains the point (last in paint order wins).                                                                                                           |
| [`isDirectChildOfContained`](#isdirectchildofcontained)     | function | True when the item's direct parent is a contained container.                                                                                                                     |
| [`isInsideLockedSubtree`](#isinsidelockedsubtree)           | function | True when the item or any ancestor is locked.                                                                                                                                    |
| [`markLockedItems`](#marklockeditems)                       | function | Mark items whose node is locked as `policy.movement: 'locked'` so solvers treat them as walls.                                                                                   |
| [`projectItemsToRoot`](#projectitemstoroot)                 | function | Project items expressed in a container's rendered canvas into root coordinates using the same pipeline `flattenLayout` uses.                                                     |
| [`projectItemToRoot`](#projectitemtoroot)                   | function | Root coordinates of one item after projecting `contextItems`.                                                                                                                    |
| [`renderLayoutForRect`](#renderlayoutforrect)               | function | Project a container's authored layout into the rectangle it renders in.                                                                                                          |
| [`rootPointToContainer`](#rootpointtocontainer)             | function | Convert a root-relative point into a container's rendered canvas coordinates.                                                                                                    |
| [`scaleSizeBetweenContainers`](#scalesizebetweencontainers) | function | Translate a size authored in `source`'s canvas units to `target`'s so it covers the same number of root pixels.                                                                  |
| [`toRenderedLayout`](#torenderedlayout)                     | function | Build the layout to persist after a solve inside `container`: the canvas is rebased to the container's rendered size and the items are projected into it.                        |

## Functions

### compactLayout

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L585">
    core/nested/index.ts:585
  </a>
</span>

Shrink items vertically until the layout fits a bounded canvas. Authored
gaps between rows are preserved, flexible items shrink proportionally down
to `minH`, rigid items keep their height. Horizontal geometry is untouched.

```ts
export function compactLayout<T>(
  layout: GridLayout<T>,
  options: CompactOptions = {},
): CompactResult<T>
```

### findContainerAt

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L313">
    core/nested/index.ts:313
  </a>
</span>

Deepest container that accepts children and contains the point.

```ts
export function findContainerAt<TNode>(
  layout: FlatLayout<TNode>,
  point: GridPoint,
  options: FindContainerOptions = {},
): FlatItem<TNode> | null
```

### findFirstUnlockedAncestor

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L384">
    core/nested/index.ts:384
  </a>
</span>

Nearest ancestor (or the item itself) that is outside every locked subtree.

```ts
export function findFirstUnlockedAncestor<TNode>(
  layout: FlatLayout<TNode>,
  itemId: string,
): FlatItem<TNode> | undefined
```

### flattenLayout

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L177">
    core/nested/index.ts:177
  </a>
</span>

Flatten a tree of nested layouts into root-relative rectangles. Every
container's children are projected into the container's rendered rect.

```ts
export function flattenLayout<TNode = GridNode>(
  root: TNode,
  rootRect: GridRect,
  options: FlattenOptions<TNode> = {},
): FlatLayout<TNode>
```

### getAncestors

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L341">
    core/nested/index.ts:341
  </a>
</span>

Ancestors of an item from parent to root.

```ts
export function getAncestors<TNode>(layout: FlatLayout<TNode>, itemId: string): FlatItem<TNode>[]
```

### getDescendants

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L354">
    core/nested/index.ts:354
  </a>
</span>

All descendants of an item in paint order.

```ts
export function getDescendants<TNode>(layout: FlatLayout<TNode>, itemId: string): FlatItem<TNode>[]
```

### hitTest

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L290">
    core/nested/index.ts:290
  </a>
</span>

Deepest item whose rect contains the point (last in paint order wins).

```ts
export function hitTest<TNode>(layout: FlatLayout<TNode>, point: GridPoint): FlatItem<TNode> | null
```

### isDirectChildOfContained

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L397">
    core/nested/index.ts:397
  </a>
</span>

True when the item's direct parent is a contained container.

```ts
export function isDirectChildOfContained<TNode>(layout: FlatLayout<TNode>, itemId: string): boolean
```

### isInsideLockedSubtree

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L374">
    core/nested/index.ts:374
  </a>
</span>

True when the item or any ancestor is locked.

```ts
export function isInsideLockedSubtree<TNode>(layout: FlatLayout<TNode>, itemId: string): boolean
```

### markLockedItems

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L410">
    core/nested/index.ts:410
  </a>
</span>

Mark items whose node is locked as `policy.movement: 'locked'` so solvers
treat them as walls. Returns the input when nothing changes.

```ts
export function markLockedItems<TNode>(
  items: readonly GridItem[],
  layout: FlatLayout<TNode>,
): readonly GridItem[]
```

### projectItemsToRoot

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L433">
    core/nested/index.ts:433
  </a>
</span>

Project items expressed in a container's rendered canvas into root
coordinates using the same pipeline [`flattenLayout`](#flattenlayout) uses. Pass a full
solver result so gap preservation sees every neighbor.

```ts
export function projectItemsToRoot<TNode>(
  container: FlatItem<TNode>,
  items: readonly GridItem[],
): Map<string, GridRect>
```

### projectItemToRoot

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L465">
    core/nested/index.ts:465
  </a>
</span>

Root coordinates of one item after projecting `contextItems`.

```ts
export function projectItemToRoot<TNode>(
  container: FlatItem<TNode>,
  item: GridItem,
  contextItems: readonly GridItem[],
): GridRect
```

### renderLayoutForRect

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L152">
    core/nested/index.ts:152
  </a>
</span>

Project a container's authored layout into the rectangle it renders in.
Returns a layout whose canvas matches `rect` (with the node's padding) and
whose items are in that canvas's coordinates.

```ts
export function renderLayoutForRect(
  layout: GridLayout,
  rect: GridSize,
  padding: Partial<GridPadding> | undefined,
  gap: number,
): GridLayout
```

### rootPointToContainer

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L487">
    core/nested/index.ts:487
  </a>
</span>

Convert a root-relative point into a container's rendered canvas
coordinates. Returns `null` for leaves.

```ts
export function rootPointToContainer<TNode>(
  container: FlatItem<TNode>,
  point: GridPoint,
  canvas: GridCanvas | null = container.layout?.canvas ?? null,
): GridPoint | null
```

### scaleSizeBetweenContainers

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L512">
    core/nested/index.ts:512
  </a>
</span>

Translate a size authored in `source`'s canvas units to `target`'s so it
covers the same number of root pixels.

```ts
export function scaleSizeBetweenContainers<TNode>(
  source: FlatItem<TNode>,
  target: FlatItem<TNode>,
  size: GridSize,
): GridSize
```

### toRenderedLayout

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L536">
    core/nested/index.ts:536
  </a>
</span>

Build the layout to persist after a solve inside `container`: the canvas
is rebased to the container's rendered size and the items are projected
into it. Subsequent renders at the same size are then identity.

```ts
export function toRenderedLayout<TNode>(
  container: FlatItem<TNode>,
  items: readonly GridItem[],
): GridLayout
```

## Constants

### gridNodeAdapter

<span className="g-api-meta">
  const ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L71">
    core/nested/index.ts:71
  </a>
</span>

The [`GridTreeAdapter`](#gridtreeadapter) for plain [`GridNode`](#gridnode) trees. [`flattenLayout`](#flattenlayout) uses it
when no adapter is given.

```ts
export const gridNodeAdapter: GridTreeAdapter<GridNode<unknown>>
```

## Types

### CompactOptions

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L568">
    core/nested/index.ts:568
  </a>
</span>

Options for [`compactLayout`](#compactlayout).

```ts
export type CompactOptions = {
  /** Items that keep their height. Fixed-height items always do. */
  isRigid?: (item: GridItem) => boolean
}
```

| Member     | Type                             | Description                                                 |
| ---------- | -------------------------------- | ----------------------------------------------------------- |
| `isRigid?` | `((item: GridItem) =\> boolean)` | Items that keep their height. Fixed-height items always do. |

### CompactResult

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L574">
    core/nested/index.ts:574
  </a>
</span>

Result of [`compactLayout`](#compactlayout): the compacted layout and whether everything fit.

```ts
export type CompactResult<T = unknown> = {
  layout: GridLayout<T>
  /** `false` when rigid heights, minimums, and gaps exceed the canvas. */
  fits: boolean
}
```

| Member   | Type              | Description                                                       |
| -------- | ----------------- | ----------------------------------------------------------------- |
| `layout` | `GridLayout\<T\>` |                                                                   |
| `fits`   | `boolean`         | `false` when rigid heights, minimums, and gaps exceed the canvas. |

### FindContainerOptions

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L302">
    core/nested/index.ts:302
  </a>
</span>

Options for [`findContainerAt`](#findcontainerat).

```ts
export type FindContainerOptions = {
  /**
   * Containers other than `sourceId` must contain the point at least this
   * far inside their edges. Keeps edge brushes from switching targets.
   */
  inset?: number
  /** Container the interaction started in; it wins without any inset. */
  sourceId?: string
}
```

| Member      | Type     | Description                                                                                                                              |
| ----------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `inset?`    | `number` | Containers other than `sourceId` must contain the point at least this far inside their edges. Keeps edge brushes from switching targets. |
| `sourceId?` | `string` | Container the interaction started in; it wins without any inset.                                                                         |

### FlatItem

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L101">
    core/nested/index.ts:101
  </a>
</span>

One node of a flattened tree: its rectangle in root coordinates, its place in
the hierarchy, its rendered and authored layouts, and its resolved behavior flags.

```ts
export type FlatItem<TNode = GridNode> = {
  id: string
  /** Id of the enclosing container, or `null` for the root. */
  parentId: string | null
  /** 0 for the root, 1 for its children, and so on. */
  depth: number
  /** Rectangle in root coordinates. */
  rect: GridRect
  /** The node's item entry in its parent's authored layout. `null` for the root. */
  canonicalRect: GridRect | null
  /** The node's item entry in its parent's rendered layout. `null` for the root. */
  sizing: GridItem | null
  /**
   * For containers: the authored layout projected into `rect`. Solvers and
   * hit testing for children operate in this coordinate system.
   */
  layout: GridLayout | null
  /** The authored layout, untouched. `null` for leaves. */
  sourceLayout: GridLayout | null
  /** Gap used when projecting and solving this container's children. */
  gap: number
  node: TNode
  isContainer: boolean
  acceptsChildren: boolean
  locked: boolean
  contained: boolean
  scrollable: boolean
}
```

| Member            | Type           | Description                                                     |
| ----------------- | -------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `id`              | `string`       |                                                                 |
| `parentId`        | `string \\     | null`                                                           | Id of the enclosing container, or `null` for the root.                                                                             |
| `depth`           | `number`       | 0 for the root, 1 for its children, and so on.                  |
| `rect`            | `GridRect`     | Rectangle in root coordinates.                                  |
| `canonicalRect`   | `GridRect \\   | null`                                                           | The node's item entry in its parent's authored layout. `null` for the root.                                                        |
| `sizing`          | `GridItem \\   | null`                                                           | The node's item entry in its parent's rendered layout. `null` for the root.                                                        |
| `layout`          | `GridLayout \\ | null`                                                           | For containers: the authored layout projected into `rect`. Solvers and hit testing for children operate in this coordinate system. |
| `sourceLayout`    | `GridLayout \\ | null`                                                           | The authored layout, untouched. `null` for leaves.                                                                                 |
| `gap`             | `number`       | Gap used when projecting and solving this container's children. |
| `node`            | `TNode`        |                                                                 |
| `isContainer`     | `boolean`      |                                                                 |
| `acceptsChildren` | `boolean`      |                                                                 |
| `locked`          | `boolean`      |                                                                 |
| `contained`       | `boolean`      |                                                                 |
| `scrollable`      | `boolean`      |                                                                 |

### FlatLayout

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L134">
    core/nested/index.ts:134
  </a>
</span>

Result of [`flattenLayout`](#flattenlayout): every node in paint order (each parent before its
children) plus lookups by id and by parent id. The root's parent id is `null`.

```ts
export type FlatLayout<TNode = GridNode> = {
  rootId: string
  items: readonly FlatItem<TNode>[]
  itemsById: ReadonlyMap<string, FlatItem<TNode>>
  childrenByParentId: ReadonlyMap<string | null, readonly string[]>
}
```

| Member               | Type                                       | Description                      |
| -------------------- | ------------------------------------------ | -------------------------------- | --- |
| `rootId`             | `string`                                   |                                  |
| `items`              | `ReadonlyArray\<FlatItem\<TNode\>\>`       |                                  |
| `itemsById`          | `ReadonlyMap\<string, FlatItem\<TNode\>\>` |                                  |
| `childrenByParentId` | `ReadonlyMap\<string \\                    | null, ReadonlyArray\<string\>\>` |     |

### FlattenOptions

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L142">
    core/nested/index.ts:142
  </a>
</span>

Options for [`flattenLayout`](#flattenlayout).

```ts
export type FlattenOptions<TNode> = {
  /** Reads your own tree shape. Defaults to the `GridNode` adapter. */
  adapter?: GridTreeAdapter<TNode>
}
```

| Member     | Type                       | Description                                                    |
| ---------- | -------------------------- | -------------------------------------------------------------- |
| `adapter?` | `GridTreeAdapter\<TNode\>` | Reads your own tree shape. Defaults to the `GridNode` adapter. |

### GridNode

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L44">
    core/nested/index.ts:44
  </a>
</span>

Normalized tree node. Use [`GridTreeAdapter`](#gridtreeadapter) to flatten other shapes directly.

```ts
export type GridNode<TData = unknown> = {
  id: string
  /** Layout of this node's children. Present on containers. */
  layout?: GridLayout
  children?: GridNode<TData>[]
  /** Gap between children in pixels. Kept fixed during projection. */
  gap?: number
  /** Rendering padding for children. Overrides `layout.canvas.padding`. */
  padding?: Partial<GridPadding>
  behavior?: GridNodeBehavior
  data?: TData
}
```

| Member      | Type                         | Description                                                        |
| ----------- | ---------------------------- | ------------------------------------------------------------------ |
| `id`        | `string`                     |                                                                    |
| `layout?`   | `GridLayout`                 | Layout of this node's children. Present on containers.             |
| `children?` | `Array\<GridNode\<TData\>\>` |                                                                    |
| `gap?`      | `number`                     | Gap between children in pixels. Kept fixed during projection.      |
| `padding?`  | `Partial\<GridPadding\>`     | Rendering padding for children. Overrides `layout.canvas.padding`. |
| `behavior?` | `GridNodeBehavior`           |                                                                    |
| `data?`     | `TData`                      |                                                                    |

### GridNodeBehavior

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L30">
    core/nested/index.ts:30
  </a>
</span>

Per-node flags that control how a node takes part in nesting: whether it is a
container, accepts drops, confines its children, is locked, or keeps its
height during compaction. Every flag is optional.

```ts
export type GridNodeBehavior = {
  /** Treat the node as a container even without children. Defaults to `layout !== undefined`. */
  container?: boolean
  /** Whether items may be dropped into this node. Defaults to `container`. */
  acceptsChildren?: boolean
  /** Direct children cannot leave, and outside items cannot enter. */
  contained?: boolean
  /** The subtree is a wall: nothing inside moves, nothing outside enters. */
  locked?: boolean
  /** Keeps its height during compaction (content scrolls instead). */
  scrollable?: boolean
}
```

| Member             | Type      | Description                                                                              |
| ------------------ | --------- | ---------------------------------------------------------------------------------------- |
| `container?`       | `boolean` | Treat the node as a container even without children. Defaults to `layout !== undefined`. |
| `acceptsChildren?` | `boolean` | Whether items may be dropped into this node. Defaults to `container`.                    |
| `contained?`       | `boolean` | Direct children cannot leave, and outside items cannot enter.                            |
| `locked?`          | `boolean` | The subtree is a wall: nothing inside moves, nothing outside enters.                     |
| `scrollable?`      | `boolean` | Keeps its height during compaction (content scrolls instead).                            |

### GridTreeAdapter

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/nested/index.ts#L58">
    core/nested/index.ts:58
  </a>
</span>

Callbacks that let [`flattenLayout`](#flattenlayout) read any tree shape without conversion.

```ts
export type GridTreeAdapter<TNode> = {
  getId(node: TNode): string
  getChildren(node: TNode): readonly TNode[]
  getLayout(node: TNode): GridLayout | null | undefined
  getBehavior?(node: TNode): GridNodeBehavior | undefined
  getGap?(node: TNode): number | undefined
  getPadding?(node: TNode): Partial<GridPadding> | undefined
}
```

| Member         | Type                                          | Description |
| -------------- | --------------------------------------------- | ----------- | --- |
| `getId`        | `(node: TNode) =\> string`                    |             |
| `getChildren`  | `(node: TNode) =\> readonly TNode[]`          |             |
| `getLayout`    | `(node: TNode) =\> GridLayout \\              | null`       |     |
| `getBehavior?` | `((node: TNode) =\> GridNodeBehavior \\       | undefined)` |     |
| `getGap?`      | `((node: TNode) =\> number \\                 | undefined)` |     |
| `getPadding?`  | `((node: TNode) =\> Partial\<GridPadding\> \\ | undefined)` |     |
