---
title: Model
description: Model exports of gridla. Generated from the source declarations.
---

# Model

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

The public data model. Every type is a plain, serializable object; the core never reads or writes anything else. Constants and predicates here are shared by every solver.

```ts
import {
  DEFAULT_CANVAS,
  DEFAULT_SNAP_DISTANCE,
  EMPTY_PADDING,
  isFixedHeight,
  isFixedOnAxis,
  isFixedWidth,
  isGhost,
  isLocked,
  MIN_ITEM_SIZE,
} from 'gridla'
import type {
  GridAxis,
  GridBounds,
  GridCanvas,
  GridEdges,
  GridHeightMode,
  GridItem,
  GridItemConstraints,
  GridItemPolicy,
  GridItemSize,
  GridLayout,
  GridPadding,
  GridPoint,
  GridRect,
  GridResizeEdge,
  GridSize,
  GridSizeMode,
} from 'gridla'
```

| Export                                            | Kind     | Summary                                                                               |
| ------------------------------------------------- | -------- | ------------------------------------------------------------------------------------- |
| [`DEFAULT_CANVAS`](#default_canvas)               | const    |                                                                                       |
| [`DEFAULT_SNAP_DISTANCE`](#default_snap_distance) | const    | Default distance in pixels within which edges attract each other.                     |
| [`EMPTY_PADDING`](#empty_padding)                 | const    |                                                                                       |
| [`GridAxis`](#gridaxis)                           | type     |                                                                                       |
| [`GridBounds`](#gridbounds)                       | type     | Bounds a solver operates against.                                                     |
| [`GridCanvas`](#gridcanvas)                       | type     | The rectangle items are laid out in.                                                  |
| [`GridEdges`](#gridedges)                         | type     | Edge-based rectangle used for hit testing and viewport math.                          |
| [`GridHeightMode`](#gridheightmode)               | type     | How the canvas treats its vertical extent.                                            |
| [`GridItem`](#griditem)                           | type     | A laid-out item.                                                                      |
| [`GridItemConstraints`](#griditemconstraints)     | type     | Size constraints an item may carry.                                                   |
| [`GridItemPolicy`](#griditempolicy)               | type     | Solver participation policy for one item.                                             |
| [`GridItemSize`](#griditemsize)                   | type     | Size information used when creating or placing an item.                               |
| [`GridLayout`](#gridlayout)                       | type     | A canvas and the items positioned inside it.                                          |
| [`GridPadding`](#gridpadding)                     | type     | Per-side inset in pixels.                                                             |
| [`GridPoint`](#gridpoint)                         | type     |                                                                                       |
| [`GridRect`](#gridrect)                           | type     | A positioned rectangle.                                                               |
| [`GridResizeEdge`](#gridresizeedge)               | type     | Compass edge or corner used to resize an item.                                        |
| [`GridSize`](#gridsize)                           | type     |                                                                                       |
| [`GridSizeMode`](#gridsizemode)                   | type     | Which axes keep their pixel size when the layout is projected to another canvas size. |
| [`isFixedHeight`](#isfixedheight)                 | function |                                                                                       |
| [`isFixedOnAxis`](#isfixedonaxis)                 | function |                                                                                       |
| [`isFixedWidth`](#isfixedwidth)                   | function |                                                                                       |
| [`isGhost`](#isghost)                             | function |                                                                                       |
| [`isLocked`](#islocked)                           | function |                                                                                       |
| [`MIN_ITEM_SIZE`](#min_item_size)                 | const    | Smallest width or height an item may have.                                            |

## Functions

### isFixedHeight

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L155">
    core/model.ts:155
  </a>
</span>

```ts
export function isFixedHeight(item: Pick<GridItem, 'sizeMode'>): boolean
```

### isFixedOnAxis

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L159">
    core/model.ts:159
  </a>
</span>

```ts
export function isFixedOnAxis(item: Pick<GridItem, 'sizeMode'>, axis: GridAxis): boolean
```

### isFixedWidth

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L151">
    core/model.ts:151
  </a>
</span>

```ts
export function isFixedWidth(item: Pick<GridItem, 'sizeMode'>): boolean
```

### isGhost

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L143">
    core/model.ts:143
  </a>
</span>

```ts
export function isGhost(item: Pick<GridItem, 'policy'>): boolean
```

### isLocked

<span className="g-api-meta">
  function ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L147">
    core/model.ts:147
  </a>
</span>

```ts
export function isLocked(item: Pick<GridItem, 'policy'>): boolean
```

## Constants

### DEFAULT_CANVAS

<span className="g-api-meta">
  const ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L130">
    core/model.ts:130
  </a>
</span>

```ts
export const DEFAULT_CANVAS: Readonly<GridCanvas>
```

### DEFAULT_SNAP_DISTANCE

<span className="g-api-meta">
  const ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L138">
    core/model.ts:138
  </a>
</span>

Default distance in pixels within which edges attract each other.

```ts
export const DEFAULT_SNAP_DISTANCE: 24
```

### EMPTY_PADDING

<span className="g-api-meta">
  const ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L123">
    core/model.ts:123
  </a>
</span>

```ts
export const EMPTY_PADDING: Readonly<GridPadding>
```

### MIN_ITEM_SIZE

<span className="g-api-meta">
  const ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L141">
    core/model.ts:141
  </a>
</span>

Smallest width or height an item may have.

```ts
export const MIN_ITEM_SIZE: 1
```

## Types

### GridAxis

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L111">
    core/model.ts:111
  </a>
</span>

```ts
export type GridAxis = 'x' | 'y'
```

### GridBounds

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L117">
    core/model.ts:117
  </a>
</span>

Bounds a solver operates against. Derived from a canvas; `height` is `null`
for scrollable canvases.

```ts
export type GridBounds = {
  width: number
  height: number | null
  padding: GridPadding
}
```

| Member    | Type          | Description |
| --------- | ------------- | ----------- | --- |
| `width`   | `number`      |             |
| `height`  | `number \\    | null`       |     |
| `padding` | `GridPadding` |             |

### GridCanvas

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L23">
    core/model.ts:23
  </a>
</span>

The rectangle items are laid out in. Items use canvas-relative pixel coordinates.

```ts
export type GridCanvas = {
  width: number
  height: number
  padding: GridPadding
  heightMode: GridHeightMode
}
```

| Member       | Type             | Description |
| ------------ | ---------------- | ----------- |
| `width`      | `number`         |             |
| `height`     | `number`         |             |
| `padding`    | `GridPadding`    |             |
| `heightMode` | `GridHeightMode` |             |

### GridEdges

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L101">
    core/model.ts:101
  </a>
</span>

Edge-based rectangle used for hit testing and viewport math.

```ts
export type GridEdges = {
  top: number
  right: number
  bottom: number
  left: number
}
```

| Member   | Type     | Description |
| -------- | -------- | ----------- |
| `top`    | `number` |             |
| `right`  | `number` |             |
| `bottom` | `number` |             |
| `left`   | `number` |             |

### GridHeightMode

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L20">
    core/model.ts:20
  </a>
</span>

How the canvas treats its vertical extent.

- `bounded`: items must fit inside `height`.
- `scrollable`: items may extend below `height`; the canvas grows to fit.

```ts
export type GridHeightMode = 'bounded' | 'scrollable'
```

### GridItem

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L80">
    core/model.ts:80
  </a>
</span>

A laid-out item. `data` is caller-owned and passes through untouched.

```ts
export type GridItem<TData = unknown> = GridRect &
  GridItemConstraints & {
    id: string
    policy?: GridItemPolicy
    data?: TData
  }
```

| Member         | Type             | Description                                                              |
| -------------- | ---------------- | ------------------------------------------------------------------------ |
| `x`            | `number`         |                                                                          |
| `y`            | `number`         |                                                                          |
| `w`            | `number`         |                                                                          |
| `h`            | `number`         |                                                                          |
| `minW?`        | `number`         |                                                                          |
| `minH?`        | `number`         |                                                                          |
| `maxW?`        | `number`         |                                                                          |
| `maxH?`        | `number`         |                                                                          |
| `sizeMode?`    | `GridSizeMode`   |                                                                          |
| `fixedWidth?`  | `number`         | Pixel width to pin when `sizeMode` fixes the width. Falls back to `w`.   |
| `fixedHeight?` | `number`         | Pixel height to pin when `sizeMode` fixes the height. Falls back to `h`. |
| `id`           | `string`         |                                                                          |
| `policy?`      | `GridItemPolicy` |                                                                          |
| `data?`        | `TData`          |                                                                          |

### GridItemConstraints

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L67">
    core/model.ts:67
  </a>
</span>

Size constraints an item may carry.

```ts
export type GridItemConstraints = {
  minW?: number
  minH?: number
  maxW?: number
  maxH?: number
  sizeMode?: GridSizeMode
  /** Pixel width to pin when `sizeMode` fixes the width. Falls back to `w`. */
  fixedWidth?: number
  /** Pixel height to pin when `sizeMode` fixes the height. Falls back to `h`. */
  fixedHeight?: number
}
```

| Member         | Type           | Description                                                              |
| -------------- | -------------- | ------------------------------------------------------------------------ |
| `minW?`        | `number`       |                                                                          |
| `minH?`        | `number`       |                                                                          |
| `maxW?`        | `number`       |                                                                          |
| `maxH?`        | `number`       |                                                                          |
| `sizeMode?`    | `GridSizeMode` |                                                                          |
| `fixedWidth?`  | `number`       | Pixel width to pin when `sizeMode` fixes the width. Falls back to `w`.   |
| `fixedHeight?` | `number`       | Pixel height to pin when `sizeMode` fixes the height. Falls back to `h`. |

### GridItemPolicy

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L42">
    core/model.ts:42
  </a>
</span>

Solver participation policy for one item.

```ts
export type GridItemPolicy = {
  /**
   * `solid` (default): the item occupies space and blocks other items.
   * `ignore`: the item is a ghost. Solvers move, resize and place other items
   * straight through it. Useful for reserved slots and floating items.
   */
  collision?: 'solid' | 'ignore'
  /**
   * `movable` (default): the solver may push, swap, shrink or reorder the item
   * to make room for another item.
   * `locked`: the item is a wall. It still blocks, but never moves or resizes
   * as a side effect of another item's operation.
   */
  movement?: 'movable' | 'locked'
}
```

| Member       | Type          | Description |
| ------------ | ------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `collision?` | `"solid" \\   | "ignore"`   | `solid` (default): the item occupies space and blocks other items. `ignore`: the item is a ghost. Solvers move, resize and place other items straight through it. Useful for reserved slots and floating items.                   |
| `movement?`  | `"movable" \\ | "locked"`   | `movable` (default): the solver may push, swap, shrink or reorder the item to make room for another item. `locked`: the item is a wall. It still blocks, but never moves or resizes as a side effect of another item's operation. |

### GridItemSize

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L88">
    core/model.ts:88
  </a>
</span>

Size information used when creating or placing an item.

```ts
export type GridItemSize = Pick<GridItem, 'w' | 'h'> & GridItemConstraints
```

| Member         | Type           | Description                                                              |
| -------------- | -------------- | ------------------------------------------------------------------------ |
| `w`            | `number`       |                                                                          |
| `h`            | `number`       |                                                                          |
| `minW?`        | `number`       |                                                                          |
| `minH?`        | `number`       |                                                                          |
| `maxW?`        | `number`       |                                                                          |
| `maxH?`        | `number`       |                                                                          |
| `sizeMode?`    | `GridSizeMode` |                                                                          |
| `fixedWidth?`  | `number`       | Pixel width to pin when `sizeMode` fixes the width. Falls back to `w`.   |
| `fixedHeight?` | `number`       | Pixel height to pin when `sizeMode` fixes the height. Falls back to `h`. |

### GridLayout

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L91">
    core/model.ts:91
  </a>
</span>

A canvas and the items positioned inside it.

```ts
export type GridLayout<TData = unknown> = {
  canvas: GridCanvas
  items: GridItem<TData>[]
}
```

| Member   | Type                         | Description |
| -------- | ---------------------------- | ----------- |
| `canvas` | `GridCanvas`                 |             |
| `items`  | `Array\<GridItem\<TData\>\>` |             |

### GridPadding

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L7">
    core/model.ts:7
  </a>
</span>

Per-side inset in pixels.

```ts
export type GridPadding = {
  top: number
  right: number
  bottom: number
  left: number
}
```

| Member   | Type     | Description |
| -------- | -------- | ----------- |
| `top`    | `number` |             |
| `right`  | `number` |             |
| `bottom` | `number` |             |
| `left`   | `number` |             |

### GridPoint

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L96">
    core/model.ts:96
  </a>
</span>

```ts
export type GridPoint = { x: number; y: number }
```

| Member | Type     | Description |
| ------ | -------- | ----------- |
| `x`    | `number` |             |
| `y`    | `number` |             |

### GridRect

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L59">
    core/model.ts:59
  </a>
</span>

A positioned rectangle.

```ts
export type GridRect = {
  x: number
  y: number
  w: number
  h: number
}
```

| Member | Type     | Description |
| ------ | -------- | ----------- |
| `x`    | `number` |             |
| `y`    | `number` |             |
| `w`    | `number` |             |
| `h`    | `number` |             |

### GridResizeEdge

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L109">
    core/model.ts:109
  </a>
</span>

Compass edge or corner used to resize an item.

```ts
export type GridResizeEdge = 'n' | 's' | 'e' | 'w' | 'ne' | 'nw' | 'se' | 'sw'
```

### GridSize

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L98">
    core/model.ts:98
  </a>
</span>

```ts
export type GridSize = { w: number; h: number }
```

| Member | Type     | Description |
| ------ | -------- | ----------- |
| `w`    | `number` |             |
| `h`    | `number` |             |

### GridSizeMode

<span className="g-api-meta">
  type ·{' '}
  <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/core/model.ts#L39">
    core/model.ts:39
  </a>
</span>

Which axes keep their pixel size when the layout is projected to another
canvas size.

- `free` (default): both axes scale.
- `fixed-w`: width stays constant, height scales.
- `fixed-h`: height stays constant, width scales.
- `fixed`: neither axis scales.

```ts
export type GridSizeMode = 'free' | 'fixed-w' | 'fixed-h' | 'fixed'
```
