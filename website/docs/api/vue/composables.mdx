---
title: Composables
description: Composables exports of gridla/vue. Generated from the source declarations.
---

# Composables

> Generated by `website/scripts/generate-api.ts` from `packages/gridla/src`. Do not edit by hand; run `bun run generate` in `website/`.

Subscribe to slices of provider state as shallow refs, read the rendered or visible layout, and reach the imperative actions.

```ts
import { selectItemView, useGridActions, useGridInteractionState, useGridItem, useGridItemView, useGridLayout, useGridPreview, useGridSelection, useGridSourceLayout, useGridStore, useGridVisibleLayout } from 'gridla/vue'
import type { GridSliceRef } from 'gridla/vue'
```

| Export | Kind | Summary |
| --- | --- | --- |
| [`GridSliceRef`](#gridsliceref) | type | A read-only shallow ref: the value is replaced as a whole, its contents are never made reactive. |
| [`selectItemView`](#selectitemview) | function | Select the `GridItemView` of `itemId` from a state snapshot. |
| [`useGridActions`](#usegridactions) | hook | Imperative layout and selection actions. |
| [`useGridInteractionState`](#usegridinteractionstate) | hook | The gesture in progress, or `null` when idle. |
| [`useGridItem`](#usegriditem) | hook | One item as it should be painted right now. |
| [`useGridItemView`](#usegriditemview) | hook | Everything a rendered item needs, with minimal updates: the ref changes only when a rect or flag differs. |
| [`useGridLayout`](#usegridlayout) | hook | The rendered layout (projected onto the measured canvas size). |
| [`useGridPreview`](#usegridpreview) | hook | The solver's latest preview for the gesture in progress, or `null` when idle. |
| [`useGridSelection`](#usegridselection) | hook | Id of the selected item, or `null` when nothing is selected. |
| [`useGridSourceLayout`](#usegridsourcelayout) | hook | The layout the provider was given, in its own coordinates. |
| [`useGridStore`](#usegridstore) | hook | Subscribe to a slice of provider state. |
| [`useGridVisibleLayout`](#usegridvisiblelayout) | hook | The layout that should be painted right now: the preview during a gesture, else the rendered layout. |

## Functions

### selectItemView

<span className="g-api-meta">function · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L109">vue/composables.ts:109</a></span>

Select the [`GridItemView`](/api/qwik/hooks#griditemview) of `itemId` from a state snapshot.

```ts
export function selectItemView<TData>(state: GridState<TData>, itemId: string): GridItemView
```

## Hooks

### useGridActions

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L74">vue/composables.ts:74</a></span>

Imperative layout and selection actions. Stable for the provider's lifetime.

```ts
export function useGridActions<TData = unknown>(): GridActions<TData>
```

### useGridInteractionState

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L152">vue/composables.ts:152</a></span>

The gesture in progress, or `null` when idle.

```ts
export function useGridInteractionState(): GridSliceRef<GridInteraction | null>
```

### useGridItem

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L94">vue/composables.ts:94</a></span>

One item as it should be painted right now. `null` when absent. Accepts a ref or getter for the id.

```ts
export function useGridItem<TData = unknown>(
  itemId: MaybeRefOrGetter<string>,
): GridSliceRef<GridItem<TData> | null>
```

### useGridItemView

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L140">vue/composables.ts:140</a></span>

Everything a rendered item needs, with minimal updates: the ref changes only
when a rect or flag differs. Accepts a ref or getter for the id.

```ts
export function useGridItemView<TData = unknown>(
  itemId: MaybeRefOrGetter<string>,
): GridSliceRef<GridItemView>
```

### useGridLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L79">vue/composables.ts:79</a></span>

The rendered layout (projected onto the measured canvas size).

```ts
export function useGridLayout<TData = unknown>(): GridSliceRef<GridLayout<TData>>
```

### useGridPreview

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L157">vue/composables.ts:157</a></span>

The solver's latest preview for the gesture in progress, or `null` when idle.

```ts
export function useGridPreview<TData = unknown>(): GridSliceRef<GridPreview<TData> | null>
```

### useGridSelection

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L162">vue/composables.ts:162</a></span>

Id of the selected item, or `null` when nothing is selected.

```ts
export function useGridSelection(): GridSliceRef<string | null>
```

### useGridSourceLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L84">vue/composables.ts:84</a></span>

The layout the provider was given, in its own coordinates.

```ts
export function useGridSourceLayout<TData = unknown>(): GridSliceRef<GridLayout<TData>>
```

### useGridStore

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L46">vue/composables.ts:46</a></span>

Subscribe to a slice of provider state. The returned ref updates only when
the selected value changes (by `Object.is`, or `isEqual` when given). The
subscription ends with the calling component or effect scope.

```ts
export function useGridStore<TData = unknown, TSlice = GridState<TData>>(
  selector: (state: GridState<TData>) => TSlice = (state) => state as unknown as TSlice,
  isEqual: (a: TSlice, b: TSlice) => boolean = Object.is,
): GridSliceRef<TSlice>
```

### useGridVisibleLayout

<span className="g-api-meta">hook · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L89">vue/composables.ts:89</a></span>

The layout that should be painted right now: the preview during a gesture, else the rendered layout.

```ts
export function useGridVisibleLayout<TData = unknown>(): GridSliceRef<GridLayout<TData>>
```

## Types

### GridSliceRef

<span className="g-api-meta">type · <a href="https://github.com/wintercounter/gridla/blob/main/packages/gridla/src/vue/composables.ts#L22">vue/composables.ts:22</a></span>

A read-only shallow ref: the value is replaced as a whole, its contents are
never made reactive. Returned by every store composable.

```ts
export type GridSliceRef<T> = Readonly<ShallowRef<T>>
```

| Member | Type | Description |
| --- | --- | --- |
| `value` | `T` |  |
| `__@RefSymbol@8336` | `true` | Type differentiator only. We need this to be in public d.ts but don't want it to show up in IDE autocomplete, so we use a private Symbol instead. |
| `__@ShallowRefMarker@9166?` | `true` |  |
