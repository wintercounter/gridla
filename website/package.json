{
  "name": "gridla-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "generate": "bun run scripts/generate-api.ts",
    "check-links": "bun run scripts/check-links.ts",
    "check-snippets": "bun run scripts/check-snippets.ts",
    "check": "bun run generate && bun run build && bun run check-links && bun run check-snippets"
  },
  "devDependencies": {
    "@rspress/core": "2.0.21",
    "@rspress/plugin-llms": "2.0.21",
    "@rspress/plugin-sitemap": "2.0.21",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "typescript": "5.9.3"
  }
}
