{
  "// why": "Bun applies the nearest tsconfig's `paths` when resolving imports; without this file the root tsconfig would map `gridla` to packages/gridla/src instead of the installed tarball.",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "allowJs": true,
    "noEmit": true
  },
  "include": ["main.mjs"]
}
