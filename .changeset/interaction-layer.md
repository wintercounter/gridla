---
'gridla': minor
---

New `gridla/interaction` entry: the framework-neutral interaction layer (`createGridController`, `createPointerGesture`, `createTransferScope`, `observeSize`, `createGridStore`) that `gridla/react` is now built on. Adapter authors for other frameworks can build on the same layer. The React API is unchanged.
